import { InterventionPipe } from './intervention.pipe';

describe('InterventionPipe', () => {
  it('create an instance', () => {
    const pipe = new InterventionPipe();
    expect(pipe).toBeTruthy();
  });
});
